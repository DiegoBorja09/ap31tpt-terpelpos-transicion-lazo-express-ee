/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.controllers;


/**
 *
 * @author Admin
 */
public class AudioController {

    /*
     * private static File archivowav = new
     * File(FileSystems.getDefault().getPath("")
     * .toAbsolutePath() + "/src/com/firefuel/resources/beep.wav");
     * private static Clip clip;
     * private static AudioInputStream audioInputStream;
     */
    public static void beep() {
        /*
         * try {
         * NovusUtils.printLn(FileSystems.getDefault().getPath("")
         * .toAbsolutePath());
         * audioInputStream = AudioSystem.getAudioInputStream(archivowav);
         * clip = AudioSystem.getClip();
         * clip.open(audioInputStream);
         * clip.start();
         * } catch (LineUnavailableException | IOException |
         * UnsupportedAudioFileException e) {
         * System.err.println(e.getMessage());
         * }
         */
    }
}
